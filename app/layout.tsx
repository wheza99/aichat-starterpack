import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({
  subsets: ["latin"],
  variable: "--font-inter",
});

export const metadata: Metadata = {
  title: "Chat.AI - Experience the Future of AI Chat",
  description: "Chat.AI is a powerful AI chat platform offering natural conversations, advanced AI technology, and enterprise-grade security.",
  keywords: ["AI chat", "artificial intelligence", "chatbot", "machine learning", "natural language processing"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className="scroll-smooth">
      <body className={`${inter.variable} font-sans antialiased`}>
        {children}
      </body>
    </html>
  );
}
